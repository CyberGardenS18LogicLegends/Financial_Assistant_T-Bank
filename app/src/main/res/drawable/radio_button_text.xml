<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Состояние выбранной кнопки -->
    <item android:color="@color/textColorPrimary" android:state_checked="true" />
    <!-- Состояние нажатого текста -->
    <item android:color="@color/textColorPrimary" android:state_selected="true" />
    <!-- Обычное состояние кнопки -->
    <item android:color="@color/textRadioButtonInactive" />
</selector>